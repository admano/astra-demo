# ── Core API ──────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# ── Presidio (pattern recognizers + anonymizer) ───────────────────────────
presidio-analyzer>=2.2.354
presidio-anonymizer>=2.2.354

# ── HuggingFace NER (replaces spaCy) ─────────────────────────────────────
# TransformersNlpEngine uses a BERT model for contextual entity detection.
# Much more accurate than spaCy for names, locations, organisations.
#
# Model is downloaded automatically on first run (~400 MB, cached in ~/.cache/huggingface).
# Override model: HF_NER_MODEL=dslim/bert-large-NER uvicorn app.main:app --reload
#
transformers>=4.38.0
torch>=2.0.0          # CPU inference works fine; GPU optional

# TransformersNlpEngine still needs spaCy for tokenization only (no model download needed)
spacy>=3.7.0
# python -m spacy download en_core_web_sm   ← run once after install (tokenizer only, 12 MB)

# ── Swiss custom recognizers ──────────────────────────────────────────────
# No extra package — implemented in app/swiss_recognizers.py

# ── Testing ───────────────────────────────────────────────────────────────
pytest>=8.0.0
httpx>=0.27.0
